import { TestBed } from '@angular/core/testing';

import { ValidRequiredNoSpace } from './requiredWithNoSpace.validator';

describe('ValidRequiredNoSpace', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should work', () => { });
});
